【ミッション: ローカル作業ツリーの安全なクリーンアップ実行（フェーズ1）】
前回のタスク（指示書189）で立案したクリーンアップ設計案に基づき、対象を絞って安全なクリーンアップを実行してください。
※ 暴走していた無限コピージョブは、システムの再起動により完全に停止しています。

以下の手順を厳密に実行せよ。

状態退避: 指示書189の「Step 0」および「Step 1」を実行し、git statusの記録と git stash push -u によるスナップショット、および tools/instructions/tools の外部への物理コピーを実行すること。

状態復元: 「Step 2」を実行し、stash pop で作業ツリーにファイルを戻すこと。

安全な差分のコミット: 「Step 3-A」を実行し、漏れていた指示書9件（区分C）と Tactical CICなどの新成果物（区分B）を git add して個別にコミットすること。

重複ツリーのパージ: 「Step 3-B」を実行し、自己増殖していた tools/instructions/tools を物理削除し、.gitignoreに追記してコミットすること。

※絶対制約: 区分A（SSoTやSFTデータの修正）および区分E・F（サブモジュールの修正）は、内容の精査が必要なため今回の実行スコープから除外（スキップ）すること。今回は上記1〜4のみを実行せよ。