【ミッション: 監視スクリプトへの排他制御（ロックファイル）ロジックの追加】
VMのコスト保護機能（GPUアイドル監視）を維持したまま、安全なデプロイを可能にするため、監視スクリプトに「デプロイ中のシャットダウン保留」ロジックを組み込め。

Step 1: VMへの潜入とスクリプトの解析
gcloud compute ssh を使用して nazokake-l4-vm (zone: us-east1-b) に接続し、/home/takes/nazokake-evaluator/scripts/auto_shutdown.py の内容を読み取って現在の監視ロジック（GPUが0%の時間をどうカウントしているか）を解析せよ。

Step 2: ロックファイル制御の実装
スクリプトに以下の排他制御ロジックを注入し、上書き保存せよ。

チェック対象: /tmp/deployment_in_progress.lock

挙動: 上記のロックファイルが存在する場合、GPU使用率が0%であっても「シャットダウン処理をスキップする」または「アイドル継続時間のカウントをリセットする」ように分岐を追加する。

Step 3: 構文チェックと結果報告
修正後の auto_shutdown.py に対して python3 -m py_compile 等で構文エラーがないことを確認せよ。
修正前と修正後の該当箇所の差分（どのようなロジックを追加したか）をSREレポートとして提出すること。