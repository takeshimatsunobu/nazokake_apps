.venv/
.venv_ai/
__pycache__/
.aider*
audit_reports/
.env
.env.*

# シークレットファイル(instructions/006 Step 4: 物理統合時に必ず除外する)
serviceAccountKey.json
*.pem
*.key

# Large binaries and local backups
cloudflared.exe
*.gguf*
AllCode_Backup.txt

# ローカルLLMモデルデータ・重いキャッシュ(instructions/006 Step 3の除外方針を継続)
# 【2026-08-16修正】先頭スラッシュ無しの"models/"はapps/evaluator配下のどの深さの
# "models"ディレクトリにも誤爆する(パターンにアンカーが無いため)。本来の対象は
# apps/evaluator直下のLLM重みファイル置き場のみであり、apps/evaluator/backend/models/
# (Pydanticスキーマを置くPythonパッケージ)を誤って追跡対象外にしていた
# (persona_schemas.py新設時に発覚)。先頭スラッシュでこのファイルの位置(apps/evaluator/)
# 基準に固定し、直下の"models/"のみを対象にする。
/models/
llama-bin/
node_modules/
.mypy_cache/
.ruff_cache/
.firebase/
chroma_db/

**/serviceAccountKey.json
**/*.pem
**/*.key
