【ミッション: 本番ELYZA生成ジョブの消化状況の決定論的調査（Read-Only）】
本番環境（Cloud Run）からのELYZA生成リクエストが「お休みしました」と表示され、結果が返らない障害が発生しています。推測による修正は行わず、以下の手順に従い現状のファクトを調査・出力してください。

Step 1: Firestoreのジョブキュー滞留確認
Pythonの一時スクリプトを作成・実行し、Firestoreの nazokake_items コレクションにおいて、elyza_job_status が "pending" または "processing" となっているドキュメントが存在するか調査してください。存在する場合、その件数と最も古い created_at を出力してください。

Step 2: オンデマンドワーカーの稼働確認
現在のホスト環境（Linux検証サーバー）において、workers/ondemand_elyza_worker.py を実行しているプロセスが存在するか、ps コマンド等を用いて決定論的に確認してください。

Step 3: ファクトのトリアージと報告
上記の結果から、「本番Cloud RunからFirestoreへの書き込み自体が失敗している（0件）」のか、「書き込めているがワーカーが不在で滞留している（Pending蓄積）」のかを結論付けてサマリーとして出力してください。

※絶対制約: 調査用の一時スクリプトは実行後に必ず物理削除すること。FirestoreやローカルDB、既存コードに対する書き込み（状態変更）は一切行わないでください。