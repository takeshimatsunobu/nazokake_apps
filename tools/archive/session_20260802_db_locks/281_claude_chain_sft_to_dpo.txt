【ミッション: SFTからDPOへの学習連鎖（モデル引継ぎ）の完全な接合】
データフライホイールの連続性を確保するため、DPO学習の開始起点を生のベースモデルからSFTのアダプタ出力結果へ変更してください。

Step 1: DPOスクリプトのベースモデル変更 (tools/train_nazo_model.py)
tools/train_nazo_model.py を改修し、BASE_MODEL の定義を修正してください。

変更前: BASE_MODEL = "elyza:8b"

変更後: SFTステップの出力先である models/nazo_sft_lora を指す絶対パス（例: str(BASE_DIR / "models" / "nazo_sft_lora") ）へ変更すること。

Step 2: 静的解析とコミット
uv run ruff check tools/ による静的解析を通過させた後、変更したファイルを明示的に指定してGitコミットを行ってください。
※絶対制約: git add . は厳禁です。