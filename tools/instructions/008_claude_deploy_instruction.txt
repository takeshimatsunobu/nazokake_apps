サイレントエラーを解消した `backend/api/routers/generate.py` の修正（`logger.exception` への変更）をコミットし、本番環境へデプロイするタスクです。

【要求事項】
1. `generate.py` の変更を git commit してください（コミットメッセージ例: `fix: ELYZA生成パイプラインのサイレントエラー解消と可観測性の向上`）。
2. プロジェクトの標準的なデプロイ手順（`gcloud run deploy` または `gcloud builds submit` 等、既存の設定・ディレクトリ構成に基づく）に従い、`nazokake-backend` サービスを本番環境へデプロイしてください。
3. デプロイ完了後、ターミナルに表示されたデプロイ成功メッセージを抽出して報告してください。追加のコード修正やエラー原因の推測は不要です。
