【ミッション: GCPサービスアカウントキーの整合性検証と対話的な自律復旧】

バックエンドAPIが JSONDecodeError でクラッシュしています。原因として apps/evaluator/backend/serviceAccountKey.json の破損または中身の欠損が疑われます。以下の要件（Must Fix）に基づき、事実確認と復旧を行ってください。

Step 1: ファクトの確認（パース検証）
apps/evaluator/backend/serviceAccountKey.json を読み込み、Pythonの json モジュールを用いてパース可能か、および type フィールドが service_account であるかを検証してください。

Step 2: ユーザーとの対話による決定論的復旧（推測の絶対禁止）
パースに失敗した場合、あなたは絶対に推測でJSONの中身を生成してはなりません（シークレットのハルシネーション禁止）。
対話ツール（AskUser機能等）を用いて、ユーザーに「バックアップ済みの正しい serviceAccountKey.json が存在する絶対パス」を質問してください。
ユーザーから入力された絶対パスからファイルを読み込み、apps/evaluator/backend/serviceAccountKey.json へ安全に上書きコピー（shutil.copy等）してください。

Step 3: 最終検証とコミット除外の確認
上書き後、再度JSONのパース検証を行い、構文が正常であることを確認してください。
また、このファイルが .gitignore に含まれており、誤ってGitにコミットされない状態であることを確認してください。シークレットを含むため、絶対に git add やコミットを行ってはなりません。結果のサマリーのみを出力して完了とすること。