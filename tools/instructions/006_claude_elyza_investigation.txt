あなたは「なぞかけ自動評価・生成システム」のSREとして、本番環境で発生しているELYZA生成機能の障害調査（Priority 0）を担当しています。
現在、ELYZAの生成に失敗してもCloud Loggingにスタックトレースが記録されない「サイレントエラー」が発生しています。以下の2つのステップを順に実行し、根本原因となる生のエラーログ（ファクト）を抽出してください。

【Step 1: エラーハンドリングの修正（サイレントエラーの解消）】
1. apps/evaluator/backend/api/routers/generate.py （またはELYZAの呼び出しを管轄するファイル）の `process_elyza` 関数（またはそれに該当する処理）を調査してください。
2. 例外（Exception）を catch している `try-except` ブロックを見つけ、エラーを握りつぶしている箇所（単にフォールバックメッセージを返すだけの箇所）に、以下のいずれかのログ出力処理を追加してください。
   - `logger.error(f"ELYZA Error: {e}", exc_info=True)`
   - もしloggerが未設定の場合は、`import traceback` と `traceback.print_exc()` を追加してください。
3. 修正後、Linter (`uv run ruff check`) が通ることを確認してください。

【Step 2: ローカルでのエラー強制再現テスト】
1. 修正したバックエンドのローカルサーバーを起動するか、直接Pythonスクリプトから `process_elyza` 関数を呼び出すテストコードを一時的に作成して実行してください。
2. 実際にELYZAへのリクエストを発生させ、Step 1 で追加したスタックトレース（真のエラー内容）をターミナルに出力させてください。

【完了条件】
ELYZAが失敗した際の「詳細なスタックトレース（Pythonの例外メッセージ）」をターミナル出力として提示してください。推測による修正案は不要です。ファクトの抽出のみに専念してください。
