ファイルの更新が反映されていなかったようです。Option 2を採用し、指示内容（要件）を直接伝えます。

【ミッション要件：Ruffによる第0フェーズ自動修復の組み込み】
対象: `tools/nazo_agent.py`
要件:
1. 静的解析実行部（`phase1_audit` の直前など、LLMへエラーログを渡す前の適切な箇所）に、Ruffによる自動修復処理を組み込むこと。
2. `subprocess.run` を用いて、`uv run ruff check --fix <ターゲットディレクトリ>` および `uv run ruff format <ターゲットディレクトリ>` を実行するロジックを追加する。
3. この処理はサイレントに実行すること。自動修復時の標準出力や標準エラー出力はロガー（logger.debug等）に逃がし、LLMに渡すPhase 1のメインエラーログには絶対に含めない。
4. 目的は、未使用インポートやフォーマット違反等の些末なエラー（トイル）を、LLM推論前にPythonネイティブで事前消滅させること。

実装完了後、`python -m py_compile tools/nazo_agent.py` を実行して構文チェックを行い、正常に完了した結果を報告せよ。
