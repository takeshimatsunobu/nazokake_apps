【ミッション: LangGraphのターゲットモデル名の正規化】

ローカルのOllama環境に、最新モデル `gemma4:12b` が正式にインストールされました（Ollamaの仕様上、ハイフンなしのタグが正解でした）。
前回の実装時、環境にモデルが存在しなかったためにフォールバック設定された `tools/agent_graph.py` のモデル名を、正しいタグ名に修正してください。

### Step 1: モデル名の修正
1. `tools/agent_graph.py` 内の `ChatOllama` の初期化部分を確認してください。
2. `model="gemma2:9b"` や `model="qwen2.5-coder:32b"` 等にフォールバックされている箇所を、`model="gemma4:12b"` に書き換えてください。

### Step 2: 構文チェックと報告
1. 修正後、`python -m py_compile tools/agent_graph.py` を実行して構文エラーがないことを確認してください。
2. 修正が完了した旨（コミットは不要）を報告してください。
