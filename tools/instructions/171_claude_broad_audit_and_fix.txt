【ミッション: インフラコードの全体監査と非対話的SSHセッションの環境変数消失バグの恒久対応】現在、run_verification_server.ps1 経由でのデプロイが remote_setup.sh の実行時に Exit Code 127 (docker: command not found) を引き起こし、開発が完全にブロックされています。原因は、非対話的SSHセッションにおいて ~/.bashrc がロードされない仕様に対するワークアラウンド（PATHとDOCKER_HOSTのエクスポート）がGitのコミットから漏れ、コードベースから消失（退行）したためです。直ちに以下の「広範な監査」と「実装」を実行してください。インフラコードの広範な監査（Broad Audit）:tools/deploy/run_verification_server.ps1 の $remoteScript および infra/verification_env/setup_verification_env.sh を精読してください。非対話的シェルで実行されることを前提とした場合、Dockerのパス以外にも「環境変数の欠落」「パスのズレ」「権限不足」によってクラッシュする潜在的なリスクがないか、インフラ全体の整合性を徹底的に監査してください。恒久的な是正実装（Must Fix）:run_verification_server.ps1 の $remoteScript の冒頭（set -euo pipefail の直後）に、最低限以下の2行を復元してください。export PATH=$HOME/bin:$PATH
export DOCKER_HOST=unix:///run/user/$(id -u)/docker.sockさらに、上記の監査で見つかった脆弱性があれば、それも同時に修正してください。修正完了後、PowerShellの構文エラーがないことを確認し、変更したファイルをコミットしてください（git add . は厳禁）。